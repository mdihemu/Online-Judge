#include<bits\stdc++.h>
using namespace std;

int main()
{
    long long int a,b,c,d,x;

    //input
    cin >> a >> b >> c >> d;

    x = (a*b) - (c*d);

    //output
    cout << "Difference = " << x << endl;

    return 0;
}
